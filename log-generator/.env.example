# 로그 생성 설정
# systemd 서비스가 이 값을 읽어 log_generator.py 실행 인자로 사용합니다.

# 생성 모드: normal | chaos | sensitive
LOG_MODE=normal

# 로그 생성 간격 (초)
LOG_INTERVAL=1.0

# 로그 출력 파일 경로 (Fluent Bit이 감시하는 경로와 일치해야 함)
LOG_OUTPUT=/var/log/app/app.log
